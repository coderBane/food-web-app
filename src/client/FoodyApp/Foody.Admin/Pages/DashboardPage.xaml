<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:control="clr-namespace:Foody.Admin.Control"
             x:Class="Foody.Admin.Pages.DashboardPage"
             BackgroundColor="WhiteSmoke"
             Title="Dashboard">

    <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <control:TopGridView />

        <Label Grid.Row="1" Margin="30" Text="DashBoard View"  FontSize="Title"
               TextColor="Black"/>

        <control:FooterView Grid.Row="2" VerticalOptions="End" Margin="30,50"/>

    </Grid>

</ContentPage>
